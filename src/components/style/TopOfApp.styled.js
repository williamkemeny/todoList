import styled from "styled-components";

export const TopOfApp = styled.div`
  background-image: linear-gradient(
    to right,

    rgb(158, 166, 238),
    rgb(113, 189, 233)
  );
  padding: 10px;
  border-radius: 10px;
  border-bottom-left-radius: 0px;
  border-bottom-right-radius: 0px;
`;
